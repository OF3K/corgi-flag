- name: Find flag
  run: |
    echo "=== ALL OBJECTS ==="
    aws s3 ls s3://platform-bucket-009661764077-us-east-1/ --recursive

    echo "=== FLAG MATCHES ==="
    aws s3 ls s3://platform-bucket-009661764077-us-east-1/ --recursive | grep -iE '(^|/)(FLAG-[^ ]*|flag\.txt)$' || true
